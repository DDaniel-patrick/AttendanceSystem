<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>

<body>
    <h1>Welcome Home!!!</h1>
    <button><a href="/profile/view">View Profile</a></button>
    <button><a href="/attendance/add">CreateAttendance</a></button>

    <% allAttendance?.reverse()?.forEach(i => { %>
        <h3><%= i.name %></h3>
        <h4>Opened: <%= i.createdAt %>, closed:<%= i.closed==true?i.updatedAt:"Open" %></h4>
        <a href="/attendance/view/<%= i._id %>">View Attendances</a>
    <% }) %>
</body>

</html>