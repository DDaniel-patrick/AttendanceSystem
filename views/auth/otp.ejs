<!DOCTYPE html>
<html lang="en" class="dark-mode">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>COme Work Login</title>
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link rel="preconnect" href="https://fonts.googleapis.com/" />
    <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin />
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700&amp;display=swap"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700&amp;display=swap"
    />

    <link
      rel="stylesheet"
      type="text/css"
      href="/assets/css/screenc530.css?v=a2006a64c6"
    />
    <style>
      :root {
        --font-family-one: "Inter", sans-serif;
        --font-weight-one-light: 300;
        --font-weight-one-bold: 700;

        --font-family-two: "Inter", sans-serif;
        --font-weight-two-bold: 700;

        --font-family-three: "Inter", sans-serif;
        --font-weight-three-regular: 400;
        --font-weight-three-medium: 500;
        --font-weight-three-bold: 700;

        --letter-spacing: -0.01em;
      }
    </style>

    <script>
      
      function getContrast(t) {
        "use strict";
        return (
          "#" === t.slice(0, 1) && (t = t.slice(1)),
          3 === t.length &&
            (t = t
              .split("")
              .map(function (t) {
                return t + t;
              })
              .join("")),
          (299 * parseInt(t.substr(0, 2), 16) +
            587 * parseInt(t.substr(2, 2), 16) +
            114 * parseInt(t.substr(4, 2), 16)) /
            1000 >=
          128
            ? "no-white-text"
            : "is-white-text"
        );
      }
    </script>
    <link rel="canonical" href="/" />

    

    

  
    <style id="gh-members-styles">
      .gh-post-upgrade-cta-content,
      .gh-post-upgrade-cta {
        display: flex;
        flex-direction: column;
        align-items: center;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
        text-align: center;
        width: 100%;
        color: #ffffff;
        font-size: 16px;
      }

      .gh-post-upgrade-cta-content {
        border-radius: 8px;
        padding: 40px 4vw;
      }

      .gh-post-upgrade-cta h2 {
        color: #ffffff;
        font-size: 28px;
        letter-spacing: -0.2px;
        margin: 0;
        padding: 0;
      }

      .gh-post-upgrade-cta p {
        margin: 20px 0 0;
        padding: 0;
      }

      .gh-post-upgrade-cta small {
        font-size: 16px;
        letter-spacing: -0.2px;
      }

      .gh-post-upgrade-cta a {
        color: #ffffff;
        cursor: pointer;
        font-weight: 500;
        box-shadow: none;
        text-decoration: underline;
      }

      .gh-post-upgrade-cta a:hover {
        color: #ffffff;
        opacity: 0.8;
        box-shadow: none;
        text-decoration: underline;
      }

      .gh-post-upgrade-cta a.gh-btn {
        display: block;
        background: #ffffff;
        text-decoration: none;
        margin: 28px 0 0;
        padding: 8px 18px;
        border-radius: 4px;
        font-size: 16px;
        font-weight: 600;
      }

      .gh-post-upgrade-cta a.gh-btn:hover {
        opacity: 0.92;
      }
    </style>
    <script async src="https://js.stripe.com/v3/"></script>
    
    <script defer src="/public/cards.minc530.js?v=a2006a64c6"></script>
    <link
      rel="stylesheet"
      type="text/css"
      href="/public/cards.minc530.css?v=a2006a64c6"
    />
  
    <script
      defer
      src="/public/member-attribution.minc530.js?v=a2006a64c6"
    ></script>
    <style>
      @media (min-width: 1481px) {
        .hero-title {
          max-width: 1070px;
        }
      }
    </style>
    <style>
      .item.is-top.is-first .item-image {
        aspect-ratio: 4/3.05;
      }
      .subscribe-form {
        padding: 28px 34px 32px;
      }
      .subscribe-title {
        margin: 0 0 2.4vh;
      }
      @media (max-width: 1480px) {
        .subscribe-form {
          padding: 30px;
        }
      }
      @media (max-width: 1280px) {
        .subscribe-form {
          padding: 26px 2vw;
        }
      }
      @media (max-width: 1024px) {
        .subscribe-form {
          padding: 20px;
        }
      }
      .post-sidebar .subscribe-form {
        padding: 22px 25px 25px;
      }
      .footer-section .subscribe-form {
        padding: 20px 26px 28px;
      }
    </style>
    
    <style>
      body.is-scroll-banner .is-banner {
        bottom: 30px;
      }
      .is-banner {
        font-size: 24px;
        position: fixed;
        z-index: 9999;
        right: 2%;
        bottom: -100px;
        display: inline-block;
        padding: 2px 10px;
        transition-timing-function: ease;
        transition-duration: 0.5s;
        transition-property: bottom, transform;
        color: #171822;
        border-radius: 100px;
        background-color: #97fe65;
      }
      .is-banner,
      .is-banner:hover {
        color: #171822;
      }
      .is-banner:hover {
        transform: translateY(-3px);
      }
      @media (max-width: 1024px) {
        .is-banner {
          display: none !important;
        }
      }
    </style>
    <style>
      :root {
        --ghost-accent-color: #f35e63;
      }
    </style>
  </head>
  <body class="page-template page-signup custom-page">
    <script>
      !(function () {
        "use strict";
        const t = document.body,
          o = getComputedStyle(t)
            .getPropertyValue("--ghost-accent-color")
            .trim();
        o && t.classList.add(getContrast(o));
      })();
    </script>
    <div class="custom-wrap">
      <div class="custom-container">
        <div class="custom-logo-wrap">
          <div class="custom-logo global-logo is-header">
            <a sh href="/" class="is-logo"
              ><img
                src="/content/images/2022/10/reiro_white.png"
                alt="Come Work home"
               
            /></a>
          </div>
        </div>
        <div class="custom-content">
          <form method="post" action="/register/otp">
            <h1 class="custom-title">Verification</h1>
            <p><%= msg %></p>
            <input
            type="text" name="otp" id="otp" required maxlength="4" placeholder="Input Otp"
            />
            
            <button class="global-button" type="submit">Verify</button>
            
            <small class="global-question"
              ><span>Didn't receive a mail? Request one in..</span>
              <div class="countdown" id="countdown"></div></small
            >
            <form action="/register/resendOtp" method="post">
                <input type="submit" value="Resend OTP" id="resendButton" disabled>
            </form>
          </form>
          
        </div>
      </div>
      <div
        class="custom-image global-bg-image"
        style="
          background-image: url(/content/images/2022/10/pexels-jacob-moore-9600527.jpg);
        "
      ></div>
    </div>

    <script>
        var countdownDate;
        var countdownTimer;

        function startCountdown() {
            countdownDate = new Date();
            countdownDate.setMinutes(countdownDate.getMinutes() + 3); // Add 1 minute to the current time

            countdownTimer = setInterval(function () {
                var now = new Date().getTime();
                var timeRemaining = countdownDate - now;

                var minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
                var seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);

                document.getElementById("countdown").innerHTML = minutes + "m " + seconds + "s";

                if (timeRemaining < 0) {
                    clearInterval(countdownTimer);
                    document.getElementById("countdown").innerHTML = "";
                    document.getElementById("resendButton").disabled = false;
                }
            }, 1000);
        }

        function resendOTP() {
            // Simulating OTP resend functionality
            // Replace this with your actual resend OTP logic
            document.getElementById("countdown").innerHTML = "";
            document.getElementById("resendButton").disabled = true;
            startCountdown();
        }

        // document.getElementById("resendButton").addEventListener("click", resendOTP);
        startCountdown();
    </script>
  </body>

</html>




<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Page</title>
    <style>
        .countdown {
            font-size: 24px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 10px;
        }

        #resendButton {
            display: block;
            margin: 0 auto;
            text-align: center;
            padding: 10px;
            width: 120px;
            background-color: #4CAF50;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        #resendButton[disabled] {
            background-color: #aaa;
            cursor: not-allowed;
        }
    </style>
</head>

<body>
    <p style="color: red;">
        <%= msg %>
    </p>
    <h1 style="margin-left: 45%;">Enter OTP</h1>
    <form action="/register/otp" method="post" enctype="multipart/form-data" style="margin-left: 45%;">
        <label for="otp">OTP: </label>
        <input type="text" name="otp" id="otp" required maxlength="4">
        <br>
        <input type="submit" value="Submit">
    </form>
    <br>
    <div style="display: flex; justify-content: center; align-items: center; flex-direction: column;">
        <p>Didn't receive a mail? Request one in..</p>
        <div class="countdown" id="countdown"></div>
    </div>
    <form action="/register/resendOtp" method="post">
        <input type="submit" value="Resend OTP" id="resendButton" disabled>
    </form>

    <script>
        var countdownDate;
        var countdownTimer;

        function startCountdown() {
            countdownDate = new Date();
            countdownDate.setMinutes(countdownDate.getMinutes() + 3); // Add 1 minute to the current time

            countdownTimer = setInterval(function () {
                var now = new Date().getTime();
                var timeRemaining = countdownDate - now;

                var minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
                var seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);

                document.getElementById("countdown").innerHTML = minutes + "m " + seconds + "s";

                if (timeRemaining < 0) {
                    clearInterval(countdownTimer);
                    document.getElementById("countdown").innerHTML = "";
                    document.getElementById("resendButton").disabled = false;
                }
            }, 1000);
        }

        function resendOTP() {
            // Simulating OTP resend functionality
            // Replace this with your actual resend OTP logic
            document.getElementById("countdown").innerHTML = "";
            document.getElementById("resendButton").disabled = true;
            startCountdown();
        }

        // document.getElementById("resendButton").addEventListener("click", resendOTP);
        startCountdown();
    </script>
</body>

</html>